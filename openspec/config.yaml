# SDD Configuration — terminalAutobuses-v2
# Auto-generated by sdd-init | 2026-05-30

context:
  project: terminalAutobuses-v2
  stack:
    backend: Spring Boot 4.0.6 / Java 21 / Maven / MySQL 8.0 / JPA / Security / JWT
    frontend: React 19 / Vite 8 / JSX / Tailwind CSS v4 / Axios / React Router 7
  architecture: Monorepo (backend + frontend) sin git repo
  timezone: America/Caracas

strict_tdd: true

testing:
  runner: JUnit 5 (Mockito + AssertJ) via Maven
  layers:
    unit: true
    integration: true
    e2e: false
  coverage: false
  frontend: false  # No test runner detected in frontend
  quality:
    linter: ESLint 10 (frontend only)
    type_checker: false
    formatter: false

persistence:
  mode: hybrid
  openspec_path: openspec/
  engram_project: terminalautobuses-v2

phases:
  proposal:
    artifact: openspec/changes/{change_id}/proposal.md
  spec:
    artifact: openspec/changes/{change_id}/spec.md
  design:
    artifact: openspec/changes/{change_id}/design.md
  tasks:
    artifact: openspec/changes/{change_id}/tasks.json
  apply:
    artifact: openspec/changes/{change_id}/apply/
  verify:
    artifact: openspec/changes/{change_id}/verify.md
  archive:
    artifact: openspec/changes/{change_id}/archive.md

session:
  execution_mode: auto
  artifact_store: hybrid
  chained_pr_strategy: auto-forecast
  review_budget_lines: 10000
